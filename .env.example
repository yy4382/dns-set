# Example .env file for dns-set
# Copy to .env and fill in your values

# Cloudflare API Token (required)
# Get from: https://dash.cloudflare.com/profile/api-tokens
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token-here

# Preferred IP detection method (optional)
# Options: interface, api, manual
DNS_SET_IP_SOURCE=api

# Custom Caddyfile path (optional)
DNS_SET_CADDYFILE_PATH=/etc/caddy/Caddyfile

# Custom config directory (optional)
# Default: ~/.config/dns-set/
DNS_SET_CONFIG_DIR=/custom/config/path